version: '3.8'

services:
  saveflow:
    build: .
    container_name: saveflow_app
    ports:
      - "5001:5001"
    volumes:
      - ./downloads:/app/downloads
    environment:
      - PORT=5001
    restart: unless-stopped
